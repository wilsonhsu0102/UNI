import React from 'react';
import CardQueue from '../components/CardQueue'

class Home extends React.Component {
    render()  {
        return (
            <CardQueue></CardQueue>
          );
      } 
  }
export default Home;
